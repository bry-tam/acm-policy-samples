---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: ../../components

helmCharts:
  - releaseName: cluster-monitoring-config
    name: cluster-monitoring-config
    valuesFile: values.yaml
