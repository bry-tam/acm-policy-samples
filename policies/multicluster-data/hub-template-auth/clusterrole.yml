---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: multicluster-hub-auth
rules:
  - apiGroups:
      - view.open-cluster-management.io
    resources:
      - managedclusterviews
    verbs:
      - get
      - watch
      - list
