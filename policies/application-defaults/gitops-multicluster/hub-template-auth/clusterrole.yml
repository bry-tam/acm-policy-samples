---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: argo-mulitcluster-hub-auth
rules:
  - apiGroups:
      - 'authentication.open-cluster-management.io'
      - 'cluster.open-cluster-management.io'
      - ''
    resources:
      - ManagedCluster
      - ManagedServiceAccount
      - Secret
    verbs:
      - get
      - watch
      - list
