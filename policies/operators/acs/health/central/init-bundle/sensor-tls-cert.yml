apiVersion: policy.open-cluster-management.io/v1
kind: CertificatePolicy
metadata:
  name: acs-bundle-certificates
spec:
  namespaceSelector:
    include: ["stackrox"]
  labelSelector:
    init-bundle: sensor-tls
  remediationAction: inform
  severity: high
  minimumDuration: 8736h