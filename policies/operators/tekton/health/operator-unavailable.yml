kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: openshift-operators
spec:
  selector:
    matchLabels:
      name: openshift-pipelines-operator
status:
  conditions:
    - type: Available
      status: 'False'
