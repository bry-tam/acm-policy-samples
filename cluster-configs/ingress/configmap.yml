---
apiVersion: v1
data:
  infraCount: '{{ len ((lookup "machineconfiguration.openshift.io/v1" "MachineConfigPool" "" "infra").status.machineCount) | toInt }}'
kind: ConfigMap
metadata:
  name: test-ingress
  namespace: wookie-policies
